List of foods should be fetched from Firebase Cloud Firestore.

The plus button opens a basic pop-up, that takes food name and  image, and calories, time.

The calender at the topic can contain static value but should be displayed by listview.

It has to use Firebase Storage as well.

The meal count should be the length of foods that are coming from firebase.

Design should be accurate to 90 percent.